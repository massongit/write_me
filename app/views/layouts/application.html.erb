<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
<head>
  <title><%= t('common.title') %></title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= stylesheet_pack_tag 'application' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application' %>
</head>

<body>
<div class="container">
  <div class="jumbotron">
    <h1 class="display-4"><%= link_to_unless_current t('common.title'), requests_path %></h1>
    <p class="lead"><%= t('common.lead_html') %></p>
  </div>
  <% flash.each do |type, message| %>
    <div class="alert alert-<%= case type
                                when 'success', 'info', 'warning', 'danger'
                                  type
                                when 'alert'
                                  'danger'
                                else
                                  'info'
                                end %>">
      <%= message %>
    </div>
  <% end %>
  <%= yield %>
  <div class="card mt-4">
    <div class="card-header"><%= t('common.other_languages') %></div>
    <div class="card-body">
      <% I18n.available_locales.each do |lang| %>
        <%= link_to_if I18n.locale != lang,
                       t('common.lang', locale: lang),
                       url_for(controller: controller.controller_name, action: controller.action_name, locale: lang.to_s) %>
      <% end %>
    </div>
  </div>
</div>
</body>
</html>
